server {
    listen 80;
    server_name your-domain;

    root /usr/share/nginx/html;  # Path to your built /dist folder
    index index.html index.htm;

    location / {
        try_files $uri /index.html;
    }
}
